extends ./layout
block section
    div(class='mx-auto')
        h1(class='text-center font-weight-bold text-info') Productos
    div(class='container')
        if productos.length !== 0
            table(class='table table-dark')
                thead
                    tr 
                        th(class="text-center") Nombre
                        th(class="text-center") Precio
                        th(class="text-center") Imagen
                tbody 
                    each item in productos 
                        tr 
                            td(class='text-center' style='vertical-align: middle;')= item.title
                            td(class='text-center' style='vertical-align: middle;')= item.price
                            td
                                img(src= item.thumbnail class='mx-auto' style='display: block; height: 100px;' alt="imagen de producto")
        else
            div(class='bg-danger px-2 py-3') 
                p(class='m-0 font-weight-bold text-light h2') No hay productos
        a(href='/') Ir a formulario